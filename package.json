{
  "name": "pathiel-dex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "bench": "tsx scripts/bench.ts",
    "predict": "tsx scripts/predict.ts",
    "smoke": "tsx scripts/smoke.ts",
    "test": "npm run typecheck && npm run build && npm run predict && cd contracts && forge test",
    "verify:addresses": "tsx scripts/verify-addresses.ts",
    "verify:tokens": "tsx scripts/verify-tokens.ts",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "probe:venues": "tsx scripts/probe-venues.ts",
    "backtest": "tsx scripts/backtest.ts",
    "mcp": "tsx scripts/mcp.ts",
    "scan:v4": "tsx scripts/scan-v4.ts",
    "sim:swaps": "tsx scripts/simulate-swaps.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@tanstack/react-query": "^5.90.0",
    "@wagmi/core": "^2.22.0",
    "mcp-handler": "^2.1.1",
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "viem": "^2.37.0",
    "wagmi": "^2.17.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "tsx": "^4.23.13",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  },
  "description": "On-chain route solver for Robinhood Chain and Base. Quotes every venue from pool state, solves the optimal split, executes through audited routers."
}
